# 国际象棋（Chess）设计规划

> 路线隶属 docs/PLATFORM.md。阶段一（同屏双人）= 2026-08-29 夜间子 Agent 并行开发；阶段二（AI）未排期。
> 实现模式遵循 src/games/gomoku/ 标杆。**全规则正确性是本项目的核心难点**，测试清单是验收核心。

## 一、规则规格（阶段一必须完整实现）

- **棋盘**：8×8，白在下先行。
- **基础走法**：王（一格）、后、车、象、马、兵（前进一格、起始位置可两格、斜吃）。
- **王车易位**：王与参与车均未动过（权利追踪）、之间无子、王不在被将军中、王经过与到达的格子均不受攻。
- **吃过路兵 (en passant)**：对方兵刚走两格的下一手内可用；机会即逝。
- **兵升变**：到底线必须升变；**升变自选（后/车/象/马）——本次实施，规格见第六节**。
- **将军 / 将死 / 逼和**：合法步 = 伪合法步 − 走后己王被攻的局面；无合法步时被将军=将死（负），不被将军=逼和（和）。
- **判和**：逼和、子力不足（K vs K、K+B/N vs K、K+B vs K+B 同色象）。
- **不做（本次实施范围外）**：50 回合规则、三次重复、记谱（PGN）。升变自选本次实施，见第五节。

## 二、阶段一交付（同屏双人）

- 引擎 `engine/chess.ts`：内部局面类型（棋盘 + 行棋方 + 易位权利 + en passant 目标格）、走法生成、`makeMove`、终局/和棋判定、undo（快照数组）。
- UI：Unicode 棋子（白 ♔♕♖♗♘♙ / 黑 ♚♛♜♝♞♟），8×8 明暗格（浅米/深灰绿类护眼配色），最后一手起止格高亮，被将军的王高亮警示。
- 交互：点击己方子 → 高亮合法落点（可吃子区分标记）→ 落子；chips：手数、轮次、将军提示；终局弹窗（将死/逼和/子力不足 + 胜方）；悔棋。
- 测试辅助：引擎导出一个小型局面构造函数（从简易描述设置棋子），方便搭测试局面。

## 三、测试清单（Vitest，全部须绿——这是验收核心）

1. 各兵种基础走法（含兵起始两格、斜吃无子不可走）。
2. 王车易位：长/短易位正例；各反例（权利已失、路径有子、王被将军、经过格受攻、目的地受攻）。
3. 吃过路兵：正常吃、机会窗口过期后不可吃。
4. 升变：到底线自动变后并参与后续走法。
5. 将死：学者将杀（Scholar's mate）等场景判定终局；被将军时应将步过滤。
6. 逼和：典型僵局局面判和。
7. 子力不足判和：三种组合。
8. 非法走子拒绝（走后己王送将等）；悔棋快照一致性。

## 四、阶段二规格：AI 对手（本次实施）

- **分工**：人类执白先行，AI 执黑。
- **难度**：三档，工具栏内选择，默认中等：
  1. **简单**：深度 1（仅看己方一步，子料+PST 评估）——看不见对方回应，新手可胜。
  2. **中等**：α-β 深度 3 + MVV-LVA 走法排序 + 静态搜索（叶子只延伸吃子）。
  3. **困难**：迭代加深至深度 5（预算内）+ MVV-LVA + 杀手步/历史启发 + 静态搜索。
- **评估函数**：子料（兵100/马320/象330/车500/后900/王∞）+ 标准位置分表（PST，黑白镜像）；可加轻微机动性。保持确定性、无随机。
- **性能预算**：单步 ≤3s；**主预算用节点数**（保证确定性可复现），墙钟仅兜底；迭代加深保证任意时刻有当前最优步可返回。
- **性能优化许可**：搜索路径允许新增 make/unmake 式走子/撤销、增量化攻击检测等内部优化，**不得改变引擎既有公开 API 对外行为，既有 60 个测试必须保持全绿**。
- **不做**：置换表/Zobrist（列为后续优化选项）、重复局面与 50 回合判定（已知缺口：优势下 AI 理论上可能走出和棋重复）。
- **Worker**：`chess.ai.worker.ts` 薄封装（模式同 reversi/gomoku/chinese-checkers）；UI 难度选择 + AI 回合锁盘 + "AI 思考中" chip + undoToHuman。
- **测试清单（新增，引擎层直接测 ai.ts）**：
  1. AI 输出必为合法步（多场景 × 三档）；
  2. 一步将杀必找到（构造局面，三档全测）；
  3. 必胜残局（双车杀等）困难档在预算内找到将死路径；
  4. 不白送子：构造"送后"陷阱局面，中等/困难档不选送后着法；
  5. 等价换吃：目标子有保护时 AI 敢于换吃（静态搜索生效）；
  6. 优势避逼和：构造"随手棋即逼和"的胜势局面，困难档不选导致逼和的那步；
  7. 确定性：同一局面两次求解步与节点数完全一致；
  8. 预算合规：困难档最复杂中盘局面单步 ≤3s（墙钟）；
  9. 强度梯度：固定开局自对弈 hard vs medium 若干局并报告胜率（报告性，不作硬断言）。

## 五、增量规格：升变自选（本次实施）

- **引擎**：`makeMove` 增加可选升变参数（后/车/象/马）。升变步**必须显式传入**升变子；未传时返回原状态（拒绝该步，保持纯函数"同引用拒绝"风格）。原"自动变后"逻辑与对应测试移除/改写。
- **UI**：兵到达升变线时，先弹出升变选择浮层（后/车/象/马四个按钮 + 取消），选择后完成走子；取消则回到选子前状态，可改走别的步。浮层复用全局 modal 样式风格。
- **测试清单（更新）**：
  1. 四种升变子各一例：升变后按新子的走法行动；
  2. 升变步未传升变参数 → 拒绝（同引用返回）；
  3. 升变步可选参数为非法值 → 拒绝；
  4. 原"自动变后"断言改写为"必须显式传参"。
- **范围说明**：本批次只做升变自选；AI 对手与 50 回合/三次重复仍留在后续阶段。

## 六、已知取舍（2026-08-30 更新）

- 50 回合规则、三次重复判和：仍为已知缺口（后续 AI 阶段一并处理或单独补）。
- PGN/FEN 互导：不做（内部结构保持简单）。
- 升变自选：本次实施后不再是缺口（见第五节）。

## 七、增量规格：走子可读性与棋子辨识（UX，本次实施）

> 来源：玩家实测反馈——AI 应手太快看不清发生了什么；黑白棋子区分不明显。五项改动，全部限国象目录。

1. **A1 AI 落子停顿**：Worker 回复到达后不立即落子，固定延迟 **600ms** 再应用 AI 着法。延迟期内"AI 思考中"chip 的处理实现自选（保留或先撤掉均可），但玩家必须能感知"AI 想好了，正要落子"的节奏。重开/换难度/悔棋时必须取消挂起的延迟（不落地过期着法）。
2. **A2 落子滑动动画**：AI 落子（及人类落子，尽量共用）的棋子从起点格**滑动**到终点格，时长约 250ms、缓动收尾；实现参考 src/games/peg-solitaire 的 `--dx/--dy` 滑入模式（Board 按 lastMove 加动画类）。
3. **A3 被吃棋子淡出**：被吃的棋子在原格**缩小 + 淡出**约 300ms 后消失（渲染"幽灵"元素，以着法序号作 key 触发动画，同独立钻石被吃子模式）。
4. **A5 上一手文字提示**：状态条新增 chip 显示最近一手，格式：`你：白后 d1×h5`（吃子用 ×）或 `AI：黑兵 e7-e5`。人类与 AI 的着法都显示，随新着法替换。文案由**纯函数**生成（棋子中文名 + 代数坐标），放 `ui/moveText.ts` 并配套 `ui/moveText.test.ts`：非吃子 / 吃子 / 升变（含升变子名）各一例。
5. **B1 棋子上色**：白方棋子渲染为**实心白（近 #fff）+ 深色描边**，黑方棋子**实心近黑（#1c1c26 类）+ 浅色描边**（-webkit-text-stroke 或 text-shadow 实现）；明暗两种格子上都必须一眼可辨。

### 验收标准

- AI 应手时序：思考 chip → 停顿 → 滑动动画 → 被吃子淡出 → 上一手 chip 更新，全程玩家可跟随。
- 白子/黑子在浅格与深格上截图肉眼可辨（集成阶段人工确认）。
- 既有 70 个测试保持全绿 + moveText 新增测试全绿；不触碰其他游戏与共享文件。

## 八、增量规格：复盘报告 v1（本次实施，纯前端、零服务器）

> 玩家需求：下完想知道自己输在哪。原理：着法记录（history）就在引擎状态里，分析 = 用现有 AI 搜索逐局面重评，全部在浏览器本地完成。

### 范围（v1）

1. **入口**：终局弹窗与游戏页工具栏新增"复盘本局"按钮（对局进行中也可进入，分析截至当前局面）。
2. **分析器**（纯函数，`engine/analysis.ts` + `analysis.test.ts`）：
   - `analyzeGame(initialState, moves, options) → Report`：按 history 重放，逐局面用 AI 搜索（固定参数：中等深度、小节点预算，保证速度）求"最佳着法 + 评估分"；玩家每手评分 = 该手行棋方视角的评估差。
   - 分级：🟢 最佳（≈引擎首选或 Δ≈0）/ ⚪ 好棋（小损）/ 🟡 失误（中损）/ 🔴 大错（大损：丢子级/漏杀）。阈值实现自定，但须可测试。
   - Report 数据结构含：每手的评级、评估分（转白方视角曲线）、最佳着法、一句话原因（规则化文案：丢子/送杀/错过绝杀/换子亏/平稳）。
3. **报告 UI**（`ui/Report.tsx`，游戏页内切换视图）：
   - 着法时间线（红白分栏或列表），按评级着色；
   - 评估曲线（内联 SVG 折线，白方视角）；
   - 点击任一手 → 棋盘跳转到该局面（复用 Board 渲染指定局面），并显示该手评级与原因；
   - 复盘在 Worker 里跑（扩展 chess.ai.worker 协议：`analyze` 请求 + 每手进度回报），界面有进度条（x/N）。
4. **本地保存**：报告完成后存 localStorage（key 前缀 `chess-review-`，保留最近 10 局）；提供历史列表可重看。URL 分享编码不做（记录为后续）。

### 测试清单（新增，引擎层直接测 analysis.ts）

1. 一步杀对局：报告把制胜手标为 🟢 最佳，且曲线在终局达到将死分；
2. 送后陷阱对局：送后那手被标 🔴 大错，原因含"丢后"；
3. 平稳开局序列（意大利开局前若干步）：无 🔴，多为 🟢/⚪；
4. Report 曲线数据点数 = 手数 + 1；
5. 被吃子原因文案正确（丢马/丢兵等）。

### 明确不做（v1）

- URL 分享编码、跨设备同步、云端更深分析；
- 多游戏复用（先在国象试点，跑通后推广）；
- 逐手"教练重试"交互（属教练模式第二层，另行排期）。

## 九、增量规格：选择提示与王邻格解释（UX，本次实施）

> 来源：玩家反馈——"选了棋子没有落点也不知道为什么""王想斜走但没有斜向落点"。实测确认引擎规则正确（被堵死/被牵制/受攻击格），缺的是界面对"为什么"的解释。全部限国象目录。

1. **零合法步提示**：点击己方棋子且合法落点为空时：
   - 该子**存在伪合法步但全部非法**（走开会送将）→ toast **"这枚棋子被牵制：走开会送将"**；
   - **伪合法步也为 0**（被己方棋子完全堵死）→ toast **"这枚棋子当前无路可走"**；
   - 仍进入选中态（选中高亮保留，与现状一致）。
2. **轮次提示**：点击对方棋子 → toast "现在是白方回合" / "现在是黑方回合"；终局后点击保持静默（避免与终局弹窗叠加噪音）。
3. **王邻格解释**：选中王时，8 个邻格中**不可用**的格子叠加小徽标：
   - 己方棋子占位 → **己**；
   - 空格但被对方攻击（走过去送将）→ **攻**；
   - 对方棋子但受保护（吃完会被反吃送将）→ **守**；
   - 合法落点（含吃子）不标注。徽标为小号字 + 半透明底，不拦截点击。
4. **纯函数**：提示逻辑放 `ui/hints.ts`：
   - `explainSelection(state, from) → 'pinned' | 'blocked' | null`（判定依据：伪合法步数与合法步数的关系）；
   - `kingNeighborReasons(state, kingSq) → Array<{ idx, reason: 'own' | 'attacked' | 'defended' }>`；
   - 需要引擎暴露伪合法步/攻击检测的既有能力（导出即可，不得改变既有函数行为）。
   - 配套 `ui/hints.test.ts`：
     - 初始局面选白王 → 8 邻格全部 'own'；
     - 构造牵制局面（如 e4 马、e8 车沿 e 线）→ explainSelection = 'pinned'；
     - 构造完全被堵局面 → 'blocked'；
     - 邻格空但受攻 → 'attacked'；邻格敌子受保护 → 'defended'。
5. **呈现**：toast 复用全局 `.toast` 样式，约 2.6s 自动消失（同 reversi/gomoku 模式）；徽标样式放 chess.css（`.c-badge-*`），移动端字号自适应。
6. **回归要求**：既有 88 个测试保持全绿；新增 hints 测试全绿；AI 搜索行为零影响（提示只读引擎，不改搜索）。

## 十、增量规格：教练模式 v1（评注 + 拦截重试 + 提示阶梯，本次实施）

> 来源：玩家需求"像教程一样带着我玩，提升技巧"。产品形态：对局页工具栏新增**教练模式开关**（默认关，随时切换）。开启后三机制生效，AI 对手照常。

### 机制 1：逐步评注（第一层）

- 人类每走一步，教练异步评估并给出分类评语（不阻塞继续操作）：🌟 妙手 / ✅ 好棋 / ⚠️ 失误 / ❌ 大错 + 一句话原因。
- 评估方法：复用 ai.ts 搜索——走前局面求"最优着法 + 分值"，走后局面求"实际价值"（对手视角取反），Δ = 最优分 − 实际分。
- 分级阈值复用 analysis.ts 的 `GRADE_THRESHOLDS`（Δ≤15 妙手/最佳、≤90 好、≤280 失误、>280 大错）；关键局面（局面存在将杀且玩家走出将杀 = 🌟）。
- 原因文案复用复盘的规则化文案（丢X / 送杀 / 错过绝杀 / 换子亏 / 平稳），抽公共函数共用。
- 评注在 Worker 里跑（每步约 0.3–0.5s，固定中等参数 + 小节点预算），异步补上不阻塞。

### 机制 2：大错拦截 + 重试（第二层核心）

- 人类选子后、落子前：若目标步预评估为 ❌ 大错（丢子级 / 漏掉对方一步杀），弹出**教练卡**（棋盘上方小卡片，不遮挡棋盘）："教练：这步会丢后（Q×h4），建议重试"，按钮 [🔄 重试这步] [▶️ 坚持走]。
- 重试 → 悔回该步（复用选子前状态）；坚持 → 照走并记录"坚持次数"。
- 拦截预评估走 Worker（复用 coachEval 通道），等待期棋盘锁定 ≤1.5s（超时放行，不卡玩家）。
- ⚠️/✅/🌟 不拦截，仅评注。

### 机制 3：提示阶梯

- 工具栏"提示"按钮（教练开启时可用），每按一次升一级：
  1. **泛泛提醒**：规则化文字（"注意你的象被捉""有一步将杀机会""向中心发展出子"）；
  2. **高亮关键**：高亮最佳着法的起点棋子（及被吃目标若为吃子）；
  3. **显示着法**：高亮起点+终点（即最佳着法全 disclosure）。
- 一局累计三次三级提示 → 状态条标记"教学局"（本地记录，不计战绩——战绩系统未做，先只做标记）。
- 换手/新开一局后阶梯重置。

### 技术与测试

- Worker 协议扩展：`coachEval`（评估指定着法 → 分级+原因+最佳着法）。请求 id 丢弃过期回复，同既有模式。
- UI：教练开关（toggle 高亮）、教练评语条（状态条下方，点击展开详情）、大错拦截卡、提示按钮。
- **测试清单（新增）**：
  1. 教练分级纯函数 `gradeMove`：送后局面 → ❌+丢后；一步杀局面走出杀 → 🌟；有一步杀不走 → ❌ 错过绝杀；平稳 → ✅；
  2. 提示阶梯状态机：三级递进、换手重置；
  3. 大错拦截状态机：拦截 → 重试恢复选子前；坚持 → 落子；
  4. 教练关闭时零行为变化（既有 98 测试全绿）。
- **明确不做（v1）**：复盘报告合并（各自独立）、五子棋/中国象棋推广、战术谜题库、战绩系统。

## 十一、教学深化路线（已规划，分三步实施）

> 来源：玩家反馈——教练模式只是"裁判"（判好坏），不是"老师"（教不会的人）。目标：纯新手零基础可入门，逐级学到会判断形势。实施顺序：新手学堂 → 开局课堂 → 局面判断入门。

### 第一步：新手学堂（交互式闯关，优先实施）

不是文字教程，是**闯关式互动**：每关 = 知识点 + 必须亲手完成。首批 6~8 关：

| 关 | 知识点 | 过关条件 |
|----|--------|---------|
| 1 | 认识棋盘与棋子 | 点击指定棋子，完成指定移动 |
| 2 | 兵的走法 | 分别完成直进、起始两格、斜吃 |
| 3 | 兵的升变 | 推兵到底线并选择升变子 |
| 4 | 将死的概念 | 王车配合 3 步将死孤王（照提示走） |
| 5 | 王车易位 | 完成一次短易位（点击王走过两格） |
| 6 | 吃过路兵 | 在构造局面中完成一次吃过路兵 |
| 7 | 捉子与保护 | 用受保护的子吃掉无保护的子 |
| 8 | 综合小测 | 连续完成 3 个混合任务 |

**技术方案**：关卡剧本系统。每关 = 初始局面（复用 position() 构造器）+ 目标条件（引擎可判定）+ 文案序列（说明→演示→动手→通过）。新代码主体是"关卡运行器"UI 框架（`ui/lessons/`），引擎零改动或仅微增判定函数。此基建后续复用于围棋教学（吃子教学法）。

### 第二步：开局课堂（回答"第一步出谁"）

- 内置 3~5 个经典开局体系（意大利开局、后翼弃兵等），每步讲解"为什么"（出快子、抢中心、王车易位安全）；
- 玩家偏离开局树时教练解释："这步不坏，但初学建议先完成出子"；
- 三大原理卡片贯穿：中心控制、快出轻子、王的安全。

### 第三步：局面判断入门（"怎么判断形势"）

- 复盘评估曲线上加形势标签（白优/均势/黑优，按分值区间）；
- 关键节点（复盘 🔴 处）配"为什么"的展开解释；
- 教三个信号：子料差、王的安全、子力活跃度。

## 十二、增量规格：棋盘坐标 + 吃子托盘（UX，本批实施）

### 1. 棋盘坐标（a–h / 1–8）

- 棋盘外侧标注：**下边 a–h 列标**（左→右）、**左边 8–1 行标**（上→下），白方视角；
- 实现于 Board.tsx 外框（grid 外圈或绝对定位标签），样式半透明小字，不挤压棋盘格（棋盘总尺寸不变或等比微调）；
- 学堂/对局/复盘三视图共用（Board 是同一个），自动生效。

### 2. 吃子托盘（被吃的子摆旁边）

- 棋盘两侧（或上下，移动端自适应）各一排：**白方吃掉的 黑子** / **黑方吃掉的 白子**，按被吃顺序、按价值降序排列，同型多子叠放显示 ×N 或重复字形；
- 数据：从 history 快照序列提取被吃子列表（复用 moveText/lastMoveInfo 的被吃子提取逻辑，抽公共函数到 ui/ 或直接在 Board 计算）；
- 随走子/悔棋实时更新；复盘视图同样显示。

### 3. 测试

- 被吃子提取纯函数：正常吃 / 吃过路兵 / 多子排序；
- 坐标标注为纯渲染（Board 单测不强制，人工验收）。

## 十三、围棋教学 v1（本批实施，依赖 M8 围棋引擎）

> 教学采用 go.md 第五节"吃子游戏教学法"。本批交付：**围棋学堂**（独立入口，与国象学堂同一运行器基建的第二个消费者）+ 围棋对局页。

### 关卡（v1 首批 6 关，9 路盘）

| 关 | 知识点 | 过关条件 |
|----|--------|---------|
| 1 | 气与提子 | 提掉一颗只有 1 口气的白子 |
| 2 | 打吃 | 把对方 2 口气的棋群打成 1 口气 |
| 3 | 双叫吃 | 一手同时打吃两处（构造局面） |
| 4 | 逃子（延气） | 被打吃的己方子延气逃生 |
| 5 | 反提 | 对方来提时，反提其棋群 |
| 6 | 两眼做活 | 构造局面下做出两只真眼 |

### 入口形式（"一个个调用"）

- 国象对局页：🎓 学堂（已上线）；
- 围棋对局页（M8 新增）：🎓 学堂入口 → 围棋关卡列表（消费同一 Runner 基建；Runner 需参数化棋盘渲染与规则判定，支撑 8×8 chess 与 9×9 go 两种棋盘——若耦合过深则抽 `shared/` 平台级组件）；
- 关卡进度独立存储（go-lessons-completed）。

### 明确不做（本批）

- 围棋 MCTS AI（吃子教学 AI 同批实现，见 go.md 第三节）；
- 开局课堂 / 局面判断（chess.md 第十一节第二三步，后续批次）；
- 19 路盘、让子棋、定式库。
