# 游戏大厅平台化设计（多棋类整合）

> 状态：M0–M6 阶段一全部完成上线；**AI 全家桶达成（2026-08-30）：七款中六款有 AI**（黑白棋/五子棋/中国跳棋/中国象棋/国际象棋各三档难度 + 独立钻石求解演示）；**国际象棋复盘报告 v1 已上线**（本地逐局面分析，纯前端零服务器）。待办：教练评注/提示阶梯、五子棋换边、各规则补全。本文沉淀"把多种棋类整合进同一网页"的讨论结论。
> 关联文档：[独立钻石设计](../DESIGN.md) · [README](../README.md)

## 一、背景与决策记录

独立钻石 v0.1 已交付（引擎/求解器/UI 分层，测试、构建、浏览器实测通过）。下一阶段目标：**同一网页内提供多种棋类**——打开是游戏大厅，点选进入五子棋、跳棋、独立钻石等，共享一套视觉与工程结构。

| 决策点 | 结论 | 理由 |
|--------|------|------|
| 组织方式 | 单仓库单应用，注册表模块化 | 一次部署全站生效；新棋 = 新文件夹 + 一行注册，互不干扰 |
| 路由 | 自实现 hash 路由（`#/gomoku`），不引 react-router | 约 20 行代码；hash 路由对 GitHub Pages 子路径部署零配置，刷新/分享链接均可恢复 |
| 加载 | `React.lazy` 动态导入，按棋分包 | Vite 自动切 chunk：大厅首屏只有菜单，点进哪局才下载哪局的代码 |
| 视觉 | 公共组件抽到 `shared/`，风格 token 共用 | 大厅里切换游戏是"一个产品"的观感，而非 demo 拼盘 |
| 流程 | 每种棋独立规划（设计 + 里程碑），沿用既有纪律 | 引擎先行 + 纯函数 + 单元测试 + UI 只做壳——独立钻石一次做对的方法论直接复用 |

## 二、目标目录结构

```
src/
├── games/
│   ├── registry.ts          # 游戏清单：id、名称、图标、懒加载入口
│   ├── peg-solitaire/       # 现有独立钻石整体迁入（engine/ solver/ ui/）
│   ├── gomoku/              # 五子棋：engine/ + ui/ + ai worker
│   └── checkers/            # 跳棋（变体待定）
├── shared/                  # 公共组件：按钮、弹窗、Toast、状态条、页面骨架
├── styles.css               # 全局风格 token（现文件迁移/拆分）
└── App.tsx                  # 大厅 + hash 路由分发
docs/
├── PLATFORM.md              # 本文
└── games/<id>.md            # 以后每种棋各自的设计沉淀
```

## 三、核心机制

### 3.1 游戏注册表

每个游戏模块向注册表登记一条元信息，大厅据此渲染列表：

```ts
// games/registry.ts
export interface GameModule {
  id: string;                       // 路由标识，如 'gomoku'
  name: string;                     // 大厅显示名，如 '五子棋'
  description: string;              // 一句话简介
  component: LazyExoticComponent;   // 懒加载的游戏入口组件
}

export const GAMES: GameModule[] = [
  { id: 'peg-solitaire', name: '独立钻石', description: '跳过邻子，只留一颗', component: lazy(() => import('./peg-solitaire/ui')) },
  // 新棋在这里加一行
];
```

### 3.2 大厅与路由

`App.tsx` 监听 `hashchange`，按 `#/gameId` 匹配注册表：未匹配或无 hash → 渲染大厅列表；匹配 → `<Suspense>` 包裹渲染对应游戏。URL 可分享、刷新后回到同一局棋的入口。

### 3.3 懒加载分包

每种棋一个 chunk，由 Vite 在构建时自动完成。部署方式完全不变：仍是一个静态目录推 GitHub Pages，分包只影响浏览器按需取哪些文件。

### 3.4 公共组件层

从现有 `src/ui` 抽取 `Btn / Modal / Toast / Chip / GameShell`（页面骨架）到 `shared/`，各棋 UI 只写棋盘与玩法特有的部分。颜色、圆角、阴影 token 沿用现 styles.css。

## 四、接入一款新棋的标准流程

1. `games/<id>/engine/`：纯函数棋局引擎 + Vitest 单元测试（先行，不碰 DOM）
2. `games/<id>/ui/`：useReducer 状态机 + 组件，状态全部来自引擎
3. 可选 `games/<id>/ai.worker.ts`：AI 或求解器放 Worker，不卡界面
4. `registry.ts` 加一行 → 自动出现在大厅
5. 设计沉淀到 `docs/games/<id>.md`

## 五、新棋规划检查单

规划每种棋时先回答四个问题（独立钻石的答案依次是：单人解谜 / 选子-跳 / 无 / 无和棋）：

1. **对手问题**：单人解谜、同屏双人，还是需要 AI 对手？AI 用极小极大 + α-β 剪枝 + 启发排序，放 Worker（五子棋到"业余中等水平"现实可行，求解器经验复用）。
2. **交互模型**：选子→跳（钻石）、点空位落子（五子棋）、拖拽/连跳（跳棋）——均属"点击棋盘"一族，可共享交互基建。
3. **变体歧义**：同名异种先定清楚（如"跳棋"＝中国跳棋六角星多人，还是国际跳棋 8×8 吃子，两者规模差数倍）。
4. **终局与和棋**：胜负判定规则、和棋条件、是否需要禁手（如五子棋三三禁手可选）。

## 六、里程碑

| 里程碑 | 内容 | 规模 | 验收标准 |
|--------|------|------|----------|
| M0 平台化 | ✅ 已完成：大厅 + hash 路由 + 注册表；peg-solitaire 迁入 `games/`；CSS 拆全局/棋专属两层（公共 React 组件暂不预抽，等第二款游戏出现真实需求再抽） | 小 | 现有 10 个测试全绿；大厅可用；钻石玩法无回归 |
| M1 五子棋 | ✅ 阶段一（15 路引擎 + 同屏双人，2026-08-29）；✅ 阶段二 AI 已上线（三档难度，12 测试，2026-08-30，互弈验证强度梯度）；换边选择未做 | 中 | ✅ |
| M2 黑白棋 | ✅ 阶段一同屏双人（13 测试）；✅ 阶段二 AI 已上线（三档难度 α-β + 终局精确穷举，20 测试，2026-08-30） | 小 | 引擎测试全绿、大厅可进、实测对局 ✅ |
| M3 中国跳棋 | ✅ 阶段一已完成上线（28 测试，2026-08-30）；✅ 阶段二 AI 已上线（三档难度贪心/前瞻/α-β+剪枝，34 测试，2026-08-30） | 中 | 同 M2 ✅ |
| M4 中国象棋 | ✅ 阶段一（36 测试）；✅ 阶段二 AI 已上线（迭代加深 α-β 深度 5，45 测试，2026-08-30） | 中偏难 | ✅；初始局面 44 步抽查与公认值一致 |
| M5 国际象棋 | ✅ 阶段一（52 测试）；✅ 升变自选（60 测试）；✅ 阶段二 AI（迭代加深 α-β，70 测试）；✅ 走子可读性 UX（81 测试）；✅ 选择提示与王邻格解释（98 测试）；✅ 复盘报告 v1（本地逐局面分析）；✅ 教练模式 v1（逐步评注+大错拦截重试+三级提示阶梯，123 测试，2026-08-30）；50 回合/三次重复未做 | 难 | ✅ |
| M6 国际跳棋 | ✅ 阶段一同屏双人已完成上线（30 测试，2026-08-30）；AI 未排期 | 中 | 同 M2 ✅ |
| M7 国象教学深化 | ✅ 第一步"新手学堂"已完成上线（关卡运行器 + 8 关 + 进度本地保存，50 新增测试，2026-09-01）；② 开局课堂 → ③ 局面判断入门 未排期。规格：chess.md 第十一节 | 中 | 纯新手可零基础入门完成全关卡 ✅ |
| M8 围棋 | 📋 已立项排期（M7 之后）：9 路中国规则引擎 + 吃子教学 AI + 同屏双人；MCTS AI 与教学模式 v2 后续。规格：docs/games/go.md | 中 | 引擎测试全绿、大厅可进、实测对局与数子 |

选五子棋打头阵的理由：它和钻石差异最大（对战 vs 解谜），能跑通即证明平台对任意棋类通用。

## 七、全局待办与技术债（2026-08-30 更新，作为唯一待办索引）

> 各游戏的细节缺口记录在 docs/games/<id>.md 的"已知取舍"，此处只做汇总索引。

**规则正确性（优先——影响对局体验）**
- 中国跳棋"蹲子死锁"：详见 chinese-checkers.md 已知取舍，需引擎规则改动；
- 国际象棋 50 回合规则与三次重复判和（chess.md 已知取舍，AI 优势下可能走出重复和棋）；
- 中国象棋长将/长捉判负与重复局面判定（xiangqi.md 已知取舍）。

**功能推广（试点已验证，照搬到其余游戏）**
- 教练模式 v1：国象 → 五子棋（"堵活三"提示最自然）→ 中国象棋（chess.md 第十节）；
- 复盘报告 v1：国象 → 其余对弈游戏（chess.md 第八节）；
- 选择提示与王邻格解释：国象 → 全部游戏（chess.md 第九节）。

**功能缺口**
- 国际跳棋 AI（见 M6）；
- 五子棋换边选择（gomoku.md 已知取舍）；
- 独立钻石多棋盘变体、音效、主题、战绩统计（承接 DESIGN.md §九）；
- 中国跳棋连跳逐孔动画（chinese-checkers.md 已知取舍）。

**技术债（不影响使用）**
- 公共 UI 组件抽取到 shared/（M0 时决定等第二款游戏出现真实需求再抽，现有三款同构 UI 已够样本）；
- GitHub Actions 的 Node 20 弃用警告：actions/checkout、setup-node、upload-pages-artifact、deploy-pages 需升级至 Node 24 目标版本；
- 韩国服务器镜像部署：方案已备（DEPLOY-SELF-HOST.md），未执行。
