# 独立钻石（Peg Solitaire）设计方案

> 单人棋类益智游戏 · Web 版 v0.1

## 一、决策记录

| 决策点 | 结论 | 理由 |
|--------|------|------|
| 技术栈 | React 18 + Vite 5 + TypeScript | 组件化好维护；构建产物为纯静态文件；引擎/界面可彻底分层 |
| 首版范围 | 核心玩法 + AI 求解器/提示/自动演示 | 先做最核心的体验闭环，变体/战绩/主题留作后续 |
| 部署 | GitHub Pages（GitHub Actions 自动发布） | 免费，push 即上线 |
| 视觉风格 | 现代简约（扁平色块、圆角、清晰高亮） | 实现成本适中，移动端观感好 |

## 二、游戏规则规格

- **棋盘**：英式 33 孔十字棋盘，嵌在 7×7 网格中。有效格：第 0/1/5/6 行取第 2–4 列，第 2/3/4 行取全部 7 列。
- **开局**：33 孔全部有子，仅中心 (3,3) 为空。
- **走法**：任一棋子沿横竖方向跳过紧邻的一颗棋子，落入其后紧邻的空位；被跳过的棋子移除。一次合法走法记为三元组 `{ from, over, to }`。
- **终局**：无任何合法走法时结束。剩 1 子即通关；剩 1 子且在中心为完美通关。
- **评分等级**（数据驱动，可配置）：

| 剩余棋子 | 等级 |
|----------|------|
| 1 且在中心 | 天才 |
| 1 | 大师 |
| 2 | 高手 |
| 3 | 优秀 |
| 4 | 良好 |
| 5 | 还不错 |
| ≥6 | 继续努力 |

## 三、总体架构

三层分离，引擎不依赖任何 DOM，求解器不阻塞界面：

```
src/
├── engine/            # 纯逻辑层（可独立测试）
│   ├── board.ts       #   棋盘几何、49 位棋盘（lo/hi 双 lane）、8 重对称变换
│   ├── rules.ts       #   走法生成、执行、终局判定
│   ├── score.ts       #   评分等级表
│   └── engine.test.ts #   Vitest 单元测试
├── solver/
│   ├── solver.ts      # DFS 求解器（纯函数，跑在 Worker 里）
│   └── solver.worker.ts
└── ui/                # React 层
    ├── useGame.ts     # useReducer 游戏状态机
    ├── Board.tsx / StatusBar.tsx / Toolbar.tsx / ResultModal.tsx
    └── App.tsx        # 组装 + Worker 通信
```

## 四、核心数据模型

- **棋盘状态**：7×7 = 49 格，用两个 32 位整数 `{ lo, hi }` 拼成 49 位位棋盘（JS 位运算只有 32 位，故拆双 lane）。`hi` 使用 bit 32–48。
- **打包键**：`packed = lo * 2^17 + hi`，49 位 < 2^53，可作为唯一 Number 用于求解器记忆化集合。
- **走法**：`{ from, over, to }`，格子索引为 `r * 7 + c`。合法走法表 `MOVES_FROM[from][]` 启动时预计算。
- **历史**：UI 层保存历史 `Bits[]` 栈，实现悔棋；引擎本身无副作用。

## 五、求解器设计（提示 / 自动演示）

- **算法**：深度优先搜索。失败记忆化：已证不可解的状态（按 8 重对称归一化后的 packed 键）入 Set，避免重复搜索；移动排序按落点距中心曼哈顿距离升序。英式棋盘可达局面总量约 2347 万，记忆化保证必然终止。
- **节点预算**：提示 300 万 / 演示 4000 万，超预算返回 `timeout`，界面如实提示"暂时算不出来"，不假装无解。
- **Worker 协议**：
  - 主线程 → Worker：`{ kind: 'hint' | 'demo', lo, hi }`
  - Worker → 主线程：`{ kind, result: { status: 'solved', moves } | { status: 'unsolvable' } | { status: 'timeout' } }`
- **提示** = 对当前局面求解，取解的首步高亮；**自动演示** = 完整解序列逐步播放。
- 说明：求解目标默认是"剩任意 1 子"；"必须剩中心"的完美模式列为后续增强。

## 六、交互与视觉规格

- **交互流**：点击棋子 → 选中并高亮所有可落空位 → 点击空位落子；再点选中子取消；触屏同路径。演示进行中锁定手动操作。
- **动画**：落子滑入（translate 变量驱动，随棋盘尺寸自适应缩放）、被吃棋子淡出、可落空位呼吸光圈、提示金圈脉冲。
- **配色 token**：底 `#f3f4fa`、面板白、主色靛蓝 `#5b5bd6`、孔位 `#e9ebf5`、提示金 `#f5a524`。圆角 + 柔和阴影，无拟物贴图。
- **响应式**：棋盘 `min(92vw, 520px)`，格子 7×7 等分，移动端可玩。
- **终局弹窗**：展示等级、剩余子数、步数；提供"再来一局"与"悔棋一步"（回到终局前继续下）。

## 七、测试策略

- 引擎单测（Vitest）：有效格数 = 33、开局 32 子且中心为空、开局恰 4 种走法、走法执行后子数 -1、非法走法拒绝、终局判定、评分表、对称变换正确性。
- 求解器性质测试：对标准开局求解成功、解逐步回放后恰好剩 1 子、明显无解局面判定正确。

## 八、部署（GitHub Pages）

1. 推送到 GitHub 仓库（建议仓库名 `peg-solitaire`）。
2. 仓库 Settings → Pages → Source 选择 **GitHub Actions**。
3. 工作流 `.github/workflows/deploy.yml` 已内置：push 到 main → 安装依赖 → 构建 → 发布。
4. Vite `base: './'` 使用相对路径，天然兼容 `https://<user>.github.io/peg-solitaire/` 子路径部署，无需按仓库名改配置。

## 九、里程碑与后续路线

v0.1（本次）= 里程碑 M1–M4 合并交付：引擎+测试 → 基础界面 → 求解器 → 部署配置。

后续候选（按价值排序）：
1. 多棋盘变体（欧式 37 孔、三角 15 孔）——引擎天然支持，主要是 UI 选择器的工作量；
2. 战绩统计与最佳纪录（localStorage）；
3. "完美模式"求解（限定最后一子落在中心）；
4. 音效、主题皮肤、残局关卡库。

> 多棋类整合（五子棋、跳棋等同站部署）的平台化方案与里程碑见 [docs/PLATFORM.md](./docs/PLATFORM.md)。
